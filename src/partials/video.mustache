<div class="container-fluid no-padding">
  <div class="row-fluid teaserline">
    <div class="col-sm-6 col-sm-offset-3">
      <video id="{{id}}" class="col-sm-12 no-padding" poster="{{poster}}" controls="">
        <source src="{{url}}" type="video/webm">
      </video>
    </div>
  </div>
</div>
