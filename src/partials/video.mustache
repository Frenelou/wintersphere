<div class="container-fluid no-padding">
  <div class="row-fluid ">
    <div class="col-sm-12 no-padding">
      <video id="{{id}}" class="col-sm-12 no-padding" poster="{{{poster}}}" controls="" muted>
        <source src="{{{url}}}" type="video/webm">
      </video>
    </div>
  </div>
</div>
